# دليل المفاهيم البرمجية والتقنية الشامل

هذا الملف يحتوي على شرح مبسط ومركز للمفاهيم التي طلبتها، مصنفاً حسب المواضيع:

---

## 1. الـ Promises في JavaScript
**ما هي؟**
هي "وعد" بعملية ستكتمل في المستقبل (غالباً عملية غير متزامنة مثل جلب بيانات من سيرفر). لها ثلاث حالات:
- **Pending (قيد الانتظار):** البداية.
- **Fulfilled (تم بنجاح):** تم تنفيذ العملية.
- **Rejected (فشل):** حدث خطأ ما.

**ما الفائدة منها؟**
تجنب مشكلة "Callback Hell" (تداخل الدوال بشكل معقد)، وتسهيل التعامل مع الأخطاء وتنظيم الكود بشكل منطقي.

**كيف تُستخدم؟**
تستخدم عبر `.then()` للنجاح و `.catch()` للفشل، أو الأسلوب الأحدث `async/await` الذي يجعل الكود يبدو وكأنه متزامن.

---

## 2. مبادئ SOLID Principles
**ما هي؟**
خمسة مبادئ في البرمجة كائنية التوجه (OOP) تهدف لجعل الكود أكثر مرونة، قابلية للصيانة، وسهولة في الفهم:
1. **S (Single Responsibility):** الكلاس يجب أن يكون له وظيفة واحدة فقط.
2. **O (Open/Closed):** الكود مفتوح للتوسيع لكنه مغلق للتعديل.
3. **L (Liskov Substitution):** الكلاسات المشتقة يجب أن تحل محل الأساسية دون مشاكل.
4. **I (Interface Segregation):** لا تجبر الكلاس على تنفيذ واجهات (Interfaces) لا يحتاجها.
5. **D (Dependency Inversion):** الاعتماد على التجريد (Abstractions) وليس على التفاصيل.

**متى تتجنبها؟**
في المشاريع الصغيرة جداً أو "البروتوتايب" حيث قد تسبب "تعقيداً زائداً" (Over-engineering) يبطئ العمل دون فائدة حقيقية.

---

## 3. الفرق بين Cookies و Sessions
**Cookies:** ملفات نصية صغيرة تُخزن في **متصفح المستخدم**. تُستخدم لتذكر التفضيلات أو تتبع الزيارات.
**Sessions:** تُخزن البيانات على **السيرفر**. يُرسل فقط "Session ID" للمتصفح.
**الأهمية:** الحفاظ على حالة المستخدم (مثل تسجيل الدخول) أثناء التنقل بين الصفحات.

---

## 4. تعلم HTML
هي لغة هيكلة صفحات الويب (HyperText Markup Language). ليست لغة برمجة، بل لغة وصفية تستخدم "الوسوم" (Tags) لتحديد العناصر (عناوين، فقرات، صور، روابط). هي حجر الأساس لأي موقع ويب.

---

## 5. Tailwind CSS
إطار عمل (Framework) يعتمد على مفهوم "Utility-first". بدلاً من كتابة CSS مخصص، تستخدم كلاسات جاهزة في HTML مباشرة (مثل `flex`, `pt-4`, `text-center`).
**الميزة:** سرعة خرافية في التطوير وتصميم متسق دون مغادرة ملف الـ HTML.

---

## 6. كورس NEXT JS (SSG vs SSR vs ISR vs SPA)
- **SPA (Single Page Application):** تحميل الصفحة مرة واحدة والتنقل يتم عبر JavaScript (مثل React العادي).
- **SSR (Server Side Rendering):** إنشاء الصفحة في السيرفر عند كل طلب (مفيد للـ SEO).
- **SSG (Static Site Generation):** إنشاء الصفحات وقت الـ Build (سريع جداً للمدونات).
- **ISR (Incremental Static Regeneration):** تحديث الصفحات الساكنة في الخلفية دون إعادة بناء المشروع كاملاً.

---

## 7. كورس لارافل (PHP Laravel)
- **Migrations:** نظام "نسخة احتياطية" لهيكلية قواعد البيانات (تعديل الجداول عبر الكود).
- **Auth Token:** وسيلة لتأمين الـ APIs (مثل JWT).
- **Request/Response:** دورة حياة الطلب من وصوله للسيرفر حتى عودة الرد للمستخدم.
- **API:** بناء خدمات برمجية يمكن لتطبيقات الموبايل أو React استهلاكها.

---

## 8. كيف تعمل الجافاسكريبت (Event Loop)
الجافاسكريبت تعمل بـ "خيط واحد" (Single-threaded). الـ **Event Loop** هو المحرك الذي يسمح لها بالقيام بمهام غير متزامنة (مثل الـ Timers أو طلبات السيرفر) عبر وضعها في "Queue" وتنفيذها عندما يفرغ الـ "Stack" الأساسي.

---

## 9. استراتيجيات Git Branching
كيف يدير الفريق العمل على الكود:
- **Git Flow:** فروع مخصصة للميزات، التطوير، والإصدارات.
- **GitHub Flow:** فرع `main` دائماً جاهز للنشر، والعمل يتم في فروع قصيرة المدى.
- **الفائدة:** تجنب تضارب الكود (Merge Conflicts) وتنظيم عملية النشر.

---

## 10. Docker و الـ Containers
**الـ Containers:** هي حاويات معزولة تحتوي على الكود وكل ما يحتاجه (Dependencies) ليعمل في أي مكان.
**الفرق:** "تعمل على جهازي" أصبحت "تعمل في الحاوية"، لذا ستعمل في أي سيرفر بنفس الطريقة.
- **Images:** القالب (Template) للحاوية.
- **Volumes:** مكان تخزين البيانات بشكل دائم خارج الحاوية.

---

## 11. الـ Auth والـ Token
بدلاً من إرسال اسم المستخدم وكلمة السر مع كل طلب، نقوم بإرسال **Token** (سلسلة مشفرة) بعد تسجيل الدخول لأول مرة. السيرفر يتأكد من هذا التوكن ليسمح بالوصول للبيانات.

---

## 12. ما هو الـ CORS؟
**Cross-Origin Resource Sharing:** نظام أمان في المتصفح يمنع المواقع من طلب بيانات من دومين مختلف إلا بإذن.
- **Preflight Request:** طلب "استطلاع" يرسله المتصفح (بصيغة OPTIONS) ليتأكد من مسموحية الطلب.

---

## 13. الـ DevOps و CI/CD
**DevOps:** ثقافة تجمع بين التطوير (Dev) والعمليات (Ops) لتسريع وتيرة العمل.
**CI (Continuous Integration):** دمج الكود وفحصه آلياً باستمرار.
**CD (Continuous Deployment):** نشر الكود آلياً للسيرفر بمجرد نجاح الفحوصات.

---

## 14. ما هو الـ DNS؟
**Domain Name System:** "دفتر عناوين" الإنترنت. يحول الأسماء (google.com) إلى أرقام IP يفهمها السيرفر.

---

## 15. الـ Migration في قواعد البيانات
هي طريقة لوصف تغييرات قاعدة البيانات في ملفات برمجية. تكمن أهميتها في تمكين الفريق من مزامنة هيكل قاعدة البيانات بسهولة دون الحاجة لملفات SQL يدوية.

---

## 16. ما هو الـ API؟
**Application Programming Interface:** وسيط يسمح لبرنامجين بالتحدث مع بعضهما البعض. مثلاً تطبيق الموبايل يتحدث مع السيرفر عبر API لجلب البيانات.

---

## 17. الـ Encryption (التشفير)
تحويل البيانات إلى صيغة غير مفهومة باستخدام "مفتاح". يمكن إعادتها لأصلها (Decryption) إذا توفر المفتاح. (ثنائي الاتجاه).

---

## 18. الـ Hashing (الهاش)
تحويل البيانات إلى "بصمة" رقمية ثابتة الطول. **مستحيل** إعادتها لأصلها. يستخدم غالباً لحماية كلمات المرور (أحادي الاتجاه).
